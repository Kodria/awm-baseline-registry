# UI Screen Detection

Apply this only when all signals hold: direct interaction, new screens or significant layout, and enough visual complexity. After design approval, ask whether to use UI design. If accepted, add `## UI Screens` with a table whose initial Status is exactly `pending`; otherwise omit the section and route to `writing-plans`.
