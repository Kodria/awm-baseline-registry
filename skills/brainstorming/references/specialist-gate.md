# Specialist Gate

Before approaches, publish a visible verdict for architecture (`architecture-advisor`), technology (`technology-evaluator`), and NFRs (`nfr-checklist-generator`). Every approach message MUST open with all three visible verdicts. Consult a specialist in contextual mode when significant; otherwise explicitly say not applicable or trivial. Integrate applicable findings into the design.
