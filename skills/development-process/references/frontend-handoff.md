# Frontend Handoff

For UI Design pending or `**Design artifacts:**`, verify `ui-design` and `frontend-craft` in the supported shared/project skill locations. If either is absent, stop: install the `frontend` bundle with `awm update && awm init`, then resume. Do not improvise the phase without the required skill.
